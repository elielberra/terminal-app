services:
  # terminal-app-dev:
  #   container_name: terminal-app-dev
  #   build:
  #     context: ./terminal-app
  #     dockerfile: Dockerfile.dev
  #   volumes:
  #     - ./terminal-app:/app
  #   env_file:
  #     - ./terminal-app/.env.dev
  #   ports:
  #     - "8080:8080"

  #   security_opt:
  #     # - apparmor:terminal-app
  #     - no-new-privileges:true
  #     # - seccomp:./seccomp-deny-connect.json

  #   # cap_drop:
  #   #   - ALL
  terminal-app-compiled:
    container_name: terminal-app-compiled
    build:
      context: ./terminal-app
      dockerfile: Dockerfile.dev.compiled
    volumes:
      - ./terminal-app:/app
      - ./videos:/app/videos
    env_file:
      - ./terminal-app/.env.dev
    ports:
      - "8080:8080"
    # Give container access to host sound device  
    devices:
      - "/dev/snd:/dev/snd"
    # Allow container user to use /dev/snd  
    group_add:
      - "audio"               

    security_opt:
      - apparmor:terminal-app-compiled
      - no-new-privileges:true
